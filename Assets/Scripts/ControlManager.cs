using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlManager : MonoBehaviour
{
    // This will be the object that is considered the CPU in the game
    // This is where control mixing maybe PID will occur
    // This will send thrust commands to the thrusters. 
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
